<%= form_for(@revision_pasajero) do |f| %>
  <% if @revision_pasajero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@revision_pasajero.errors.count, "error") %> prohibited this revision_pasajero from being saved:</h2>

      <ul>
      <% @revision_pasajero.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :revision_id %><br>
    <%= f.number_field :revision_id %>
  </div>
  <div class="field">
    <%= f.label :pasajero_id %><br>
    <%= f.number_field :pasajero_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
